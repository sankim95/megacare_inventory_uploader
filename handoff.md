# 거래명세서 사진 → 상품 재고 갱신 프로그램 인수인계

> 문서 상태: Historical. 이 문서는 최초 요구와 2026-08-08 수정 전 코드 리뷰를 보존합니다. 현재 작업의 진입점은 `docs/00_PROJECT_CONTEXT.md`, 현재 판정은 `docs/05_CURRENT_STATUS.md`, 남은 작업은 `docs/06_BACKLOG.md`입니다. 특히 §10의 결함·테스트 수치는 수정 전 스냅샷이며 현재 상태로 사용하지 않습니다.

## 1. 목표

약국 거래명세서 사진에서 품목명·수량·사입단가를 추출하고 기존 상품리스트와 매칭한다. 사용자가 잘못 인식된 상품, `+재고`, 사입단가와 반영 여부를 수정·승인하면 원본을 보존한 채 `현재고`와 선택된 `매입단가`가 갱신된 새 Excel 파일을 제공한다.

## 2. 입력 자료

- 기준 상품리스트: `/Users/kimsangwoo/Downloads/상품리스트_2026-08-07 11_15.xlsx`
- 샘플 사진 1: `/var/folders/mv/dj440zt16s12g5kyg5_yqhdw0000gn/T/codex-clipboard-9100a7a1-28a1-428f-8e9e-46ce0f1876af.png`
- 샘플 사진 2: `/var/folders/mv/dj440zt16s12g5kyg5_yqhdw0000gn/T/codex-clipboard-b30121a5-f1dc-420a-b26c-7d8ff989279b.png`
- 샘플 사진 3: `/var/folders/mv/dj440zt16s12g5kyg5_yqhdw0000gn/T/codex-clipboard-4122d898-a305-488b-93c7-eea19f93b307.png`
- Excel: `Sheet`, 약 8,563행/52열. 핵심 열은 `상품코드`, `상품명`, `규격`, `현재고`, `매입단가`, `공급사코드`, `공급사`이다.

## 3. 확정된 업무 규칙

1. 최종 `공급처`는 사진의 공급자가 아니라 **매칭된 기존 상품 행의 Excel `공급사`**이다. 사진 공급자는 `사진 공급자`로 별도 보관한다.
2. 사진의 품목 행마다 `품목명`, `수량`, `단가`, `금액`을 추출한다. `+재고` 초깃값은 사진의 수량이며 사용자가 수정할 수 있다.
3. `최종재고 = 기준 현재고 + 해당 상품에 승인된 +재고 합계`이다. 동일 상품이 여러 사진/행에 있으면 상품코드별로 먼저 합산한다.
4. 사용자는 OCR 품목명, 매칭 상품, `+재고`, 사진 사입단가, 승인/제외, 단가 반영 여부를 수정할 수 있다.
5. **기존 매입단가와 사진 사입단가가 다르면 `매입단가 반영`을 기본 선택한다.** 사용자가 해제할 수 있다.
   - 유효한 숫자 단가가 있고 기존 상품 매칭이 완료된 경우에 적용한다.
   - 단가가 같으면 기본 미선택한다.
   - 단가 누락/인식 실패 시 선택을 비활성화하고 검수 필요로 표시한다.
   - 단가 차이가 크거나 OCR 신뢰도가 낮아도 경고를 표시하되, 유효한 숫자라면 기본 선택 규칙은 유지한다. 최종 반영은 사람의 승인 후에만 한다.
6. 승인된 행은 `현재고`를 갱신한다. `매입단가 반영`이 선택된 행은 `매입단가`도 사진 단가로 갱신한다. 가중평균이 아니라 이번 명세서의 최근 입고단가로 교체한다.
7. 미매칭 품목은 자동 신규 등록하지 않는다. 사용자가 기존 상품을 선택하거나 제외해야 한다.
8. 원본 Excel은 수정하지 않고 `상품리스트_입고반영_YYYYMMDD_HHMMSS.xlsx` 복사본을 만든다. 기존 행 순서·열·서식과 승인 대상 외 셀을 보존한다.

## 4. 검수 화면

사진/선택 영역을 왼쪽, 편집 가능한 품목표를 오른쪽에 둔다. 품목표 필드:

- 원본 파일, 사진 공급자, 거래일자, 명세서번호
- OCR 품목명·규격·수량·단가·금액
- 매칭 상품 검색/선택
- 상품코드, Excel 상품명, Excel 공급사
- 기준 현재고, `+재고`, 최종재고
- 기존 매입단가, 사진 사입단가, 가격 차이
- `재고 반영`, `매입단가 반영`, 승인/보류/제외, 경고, 메모

매칭 상품 검색 결과에는 상품코드·상품명·규격·공급사·현재고·매입단가를 함께 표시한다. 상품명 중복 시 자동 확정하지 말고 상품코드 기준으로 사람이 선택하게 한다. 사용자가 상품 또는 `+재고`를 바꾸면 공급사·현재고·최종재고·단가 비교를 즉시 다시 계산한다.

## 5. 추출·매칭·검증

- 사진 회전, 원근, 문서 영역을 보정한 후 공급자 영역과 품목표를 구조화 추출한다.
- 매칭 우선순위: 바코드/KD/상품코드 → 정규화한 상품명+규격/함량 → 유사 상품 후보.
- 가격은 보조 신호로만 사용한다. 공급사 불일치만으로 매칭을 거절하지 않는다.
- 검증: `수량 × 단가 = 금액`, 품목 합계와 문서 합계, 중복 명세서, 중복 상품 후보, 비정상 가격 차이, 도장/필기 겹침.
- `[묶음10+1]`, `세트` 등은 사진 수량을 그대로 `+재고`에 넣되 경고를 표시하고 사람이 실제 재고 단위에 맞게 수정한다.
- 명세서번호·거래일자·공급자·파일 해시·품목/금액 조합으로 중복 반영을 경고/차단한다.

## 6. 출력 Excel

1. 기존 `Sheet`: 승인된 상품코드의 `현재고` 갱신. 선택된 경우 `매입단가` 갱신.
2. `입고반영내역`: 원본 파일, 문서정보, OCR 원문, 최종 매칭 상품, Excel 공급사, 변경 전/후 재고, 기존/사진 단가, 단가 반영 여부, 승인자/시각, 메모.
3. `검수제외내역`: 미매칭·보류·제외 행과 사유.

## 7. 샘플 확인 포인트

- 사진 2: `마운자로프리필드펜주 5mg/0.5mL`, 수량 2, 단가 369,307원. 사진 공급자와 무관하게 최종 공급처는 매칭된 Excel 상품의 공급사(현재 데이터상 `백제약품`)를 사용한다.
- 사진 3: `바로타민 kids 미네랄`, 수량 8, 사진 단가 18,000원. 기존 Excel 매입단가가 20,000원이므로 `매입단가 반영`이 기본 선택되어야 한다.
- 제공 사진 3장의 모든 품목 행이 검수 화면에 나타나야 한다. 회전·원근 왜곡을 허용한다.

## 8. 권장 MVP 형태

- 약국 한 곳, 단일 사용자용 로컬 웹앱. 로그인/다중 사용자/클라우드 DB는 제외한다.
- 최신 상품리스트와 여러 사진 업로드 → 한 장씩 진행률 표시 → 검수/승인 → Excel 다운로드.
- 로컬 이력 저장소로 작업 상태와 중복 반영 정보를 보관한다.
- 초기에는 범용 비전 모델 기반 구조화 추출을 사용하고, 실제 오류가 쌓인 뒤 공급사별 별칭/보정 규칙을 추가한다.

## 9. 완료 기준

- 모든 추출 행을 사람이 수정·승인/제외할 수 있다.
- 잘못 매칭된 상품을 기존 상품 검색으로 교체할 수 있다.
- `+재고` 수정 시 상품별 최종재고가 즉시 재계산된다.
- 기존 단가와 사진 단가가 다르면 단가 반영이 기본 선택되고 사용자가 해제할 수 있다.
- 승인되지 않은 행은 어떤 값도 갱신하지 않는다.
- 동일 상품 여러 행을 중복 계산하지 않고 합산한다.
- 동일 명세서의 재반영을 차단한다.
- 출력 파일의 승인 대상 외 기존 데이터와 서식은 변하지 않는다.
- 원본 Excel은 항상 보존되고 모든 변경은 이력 시트에서 추적 가능하다.

## 10. 코드 리뷰 인수인계 (2026-08-08)

### 10.1 리뷰 범위와 현재 판정

- `PRD.md`를 기준으로 백엔드, 프런트엔드, 데이터베이스 마이그레이션, 테스트를 전체 검토했다.
- 현재 디렉터리는 Git 저장소가 아니므로 특정 커밋의 diff가 아니라 현재 구현 전체를 검토했다.
- 판정: **출시 보류**. P0는 없지만 데이터 정합성 또는 잘못된 완료 상태로 이어질 수 있는 P1 세 건이 있다.
- 리뷰 과정에서는 소스 코드를 변경하지 않았다.

### 10.2 수정 우선순위

아래 순서대로 수정하고 각 항목에 적힌 회귀 테스트를 추가한다. 인접 코드를 함께 리팩터링하거나 요구사항을 확장하지 않는다.

#### P1-1. 추출 중인 작업의 모든 변경을 서버에서 차단한다

- 주요 위치: `backend/app/services/job_mutations.py:13`, `backend/app/services/extraction.py:47`
- 현재 문제:
  - 변경 차단 상태가 `EXPORTING`, `COMPLETED`뿐이라 `EXTRACTING` 중에도 Excel 교체, 사진 업로드·삭제, 품목 수정과 재추출 요청이 가능하다.
  - 재추출 요청이 진행 중인 문서를 건너뛴 뒤 작업 상태를 먼저 `REVIEWING`으로 바꿀 수 있다.
  - 추출 도중 문서를 삭제하면 실행 중인 추출이 실패하거나 작업이 `EXTRACTING`에 고착될 수 있다.
- 확인된 재현: 작업 상태를 `EXTRACTING`으로 만든 뒤 `DELETE /api/documents/{document_id}`를 호출하면 `204`가 반환되고 문서가 삭제된다.
- 최소 수정 방향:
  - 공통 mutation guard에 `EXTRACTING`을 포함한다.
  - 추출 시작도 동일한 원자적 상태 전이로 보호한다.
  - 오래된 추출 실행이 새 실행의 상태를 덮어쓰지 못하도록 실행 토큰 또는 동등한 소유권 검사를 둔다.
- 회귀 테스트:
  - `EXTRACTING` 중 Excel·사진·품목 변경과 중복 추출이 모두 `409`로 거절되는지 확인한다.
  - 오래된 추출 요청이 종료되어도 최신 작업 상태를 바꾸지 않는지 확인한다.
  - 정상 추출 완료와 실패 복구가 각각 `REVIEWING`으로 수렴하는지 확인한다.

#### P1-2. Excel 교체 시 기존 매칭과 승인 결정을 무효화한다

- 주요 위치: `backend/app/services/jobs.py:76`, `backend/app/services/summary.py:415`
- 현재 문제:
  - 상품 인덱스만 교체되고 기존 `Item`, `PriceResolution`, 승인 상태는 유지된다.
  - 준비 상태 검사는 상품코드, Excel 행 번호, 현재고만 비교하므로 상품명·공급사·매입단가가 바뀌어도 내보내기 가능 상태가 될 수 있다.
- 확인된 재현:
  1. 기존 상품을 상품코드 `0001`, 현재고 `0`, 매입단가 `1000`으로 매칭하고 승인한다.
  2. 같은 코드·행·현재고를 유지하면서 상품명, 공급사, 매입단가를 `2000`으로 바꾼 Excel을 다시 업로드한다.
  3. 기존 품목에는 이전 상품명과 기준 단가 `1000`, `apply_purchase_price=false`가 남지만 요약은 `ready_to_export=true`가 된다.
- 최소 수정 방향:
  - 가장 단순한 MVP 대응은 추출 결과 또는 사용자 검수 데이터가 존재하면 Excel 교체를 거절하는 것이다.
  - 교체를 계속 지원해야 한다면 하나의 트랜잭션에서 전 품목을 재매칭하고 가격 결정을 초기화하며 승인 상태를 `PENDING`으로 되돌린다.
- 회귀 테스트:
  - Excel 교체 뒤 이전 상품명, 공급사, 기준 단가와 승인 상태가 남지 않는지 확인한다.
  - 교체 후 사람이 다시 검수하기 전에는 `ready_to_export=false`인지 확인한다.
  - 교체가 실패하면 기존 Excel, 상품 인덱스와 품목 상태가 함께 보존되는지 확인한다.

#### P1-3. 사용자가 사진 단가를 수정하면 반영 기본값을 다시 계산한다

- 주요 위치: `backend/app/services/matching.py:189`
- 현재 문제: 상품코드가 같으면 사진 단가가 변경되어도 이전 `apply_purchase_price` 값을 무조건 보존한다.
- 확인된 재현: 기준 단가 `1000`, 최초 사진 단가 `1000`인 품목의 사진 단가만 `1200`으로 수정하면 응답의 단가는 바뀌지만 `apply_purchase_price=false`가 유지된다.
- 최소 수정 방향:
  - 사진 단가가 변경되고 같은 요청에 반영 여부가 명시되지 않았다면 기본 비교 규칙을 다시 적용한다.
  - 사용자가 체크박스를 직접 변경한 상태를 보존해야 한다면 명시적 사용자 override 여부를 별도로 저장한다.
- 회귀 테스트:
  - 같은 가격→다른 가격은 기본 `true`, 다른 가격→같은 가격은 기본 `false`가 되는지 확인한다.
  - 단가와 체크박스를 같은 요청에서 함께 수정하면 명시한 체크박스 값이 우선하는지 확인한다.
  - 메모 등 단가와 무관한 수정은 기존 반영 결정을 바꾸지 않는지 확인한다.

#### P2-1. 문서가 없는 작업의 내보내기를 차단한다

- 주요 위치: `backend/app/services/summary.py:340`
- 현재 문제: 유효한 Excel만 있고 문서가 0개여도 blocker가 없어 `ready_to_export=true`가 된다. 실제 내보내기가 `200`으로 성공하고 작업도 `COMPLETED`가 된다.
- 수정 방향: 완료된 문서가 한 개 이상이고 검수 대상 품목이 존재하는 것을 내보내기 전제 조건으로 추가한다.
- 회귀 테스트: Excel만 업로드한 작업의 요약과 내보내기 API가 각각 blocker와 `409`를 반환하는지 확인한다.

#### P2-2. 음수 Excel 매입단가를 업로드 단계에서 처리한다

- 주요 위치: `backend/app/services/excel.py:160`, `backend/app/services/matching.py:364`
- 현재 문제: 음수 단가는 Excel 업로드에서 허용되지만 정확 매칭 시 `base_purchase_price >= 0` DB 제약에 걸려 `500`이 발생한다.
- 수정 방향: 업로드 오류로 거절하거나 값 없음과 명시적 경고로 정규화한다. 어느 정책을 사용하더라도 매칭 API에서 `500`이 발생하면 안 된다.
- 회귀 테스트: 음수 단가가 포함된 Excel의 업로드 또는 매칭 결과가 선택한 정책대로 안정적으로 처리되는지 확인한다.

#### P2-3. API 날짜를 명시적 UTC로 직렬화한다

- 주요 위치: `frontend/src/pages/JobsPage.tsx:24`, `frontend/src/pages/CompletePage.tsx:19`
- 현재 문제: SQLite에서 복원한 UTC datetime이 오프셋 없이 API에 노출되고, 브라우저는 이를 현지 시각으로 해석한다. Asia/Seoul 화면에 생성·완료 시각이 실제보다 9시간 이전으로 표시된다.
- 수정 방향: 서버에서 UTC `Z` 또는 `+00:00`가 포함된 ISO 8601 문자열을 반환한다. 프런트엔드의 임의 보정은 피한다.
- 회귀 테스트: 오프셋 포함 API 응답과 Asia/Seoul 표시 시각을 고정 시계로 검증한다.

#### P2-4. 품목 자동 저장 요청을 행별로 직렬화한다

- 주요 위치: `frontend/src/pages/ReviewPage.tsx:470`
- 현재 문제: 같은 행의 필드를 연속 수정하면 저장 요청이 겹치며, 늦게 도착한 이전 전체 행 응답이 최신 입력을 덮어쓸 수 있다.
- 수정 방향: 행별 요청 순번, 저장 큐 또는 취소 토큰 중 하나로 최신 응답만 반영한다.
- 회귀 테스트: 첫 번째 요청의 응답을 지연시킨 뒤 두 번째 요청을 먼저 완료해도 화면과 서버에 최신 두 수정이 모두 남는지 확인한다.

#### P3-1. Alembic head와 ORM 메타데이터를 동기화한다

- 주요 위치: `backend/alembic/versions/cc93b59879a2_add_completed_document_constraints.py:20`, `backend/app/models/completed_document.py:27`
- 현재 문제: 빈 DB의 `alembic upgrade head`는 성공하지만 직후 `alembic check`가 완료 문서 unique index/constraint와 enum check constraint 차이를 보고한다.
- 수정 방향: 기존 설치의 안전한 업그레이드를 유지하면서 마이그레이션 정의와 ORM 제약 표현을 동일하게 만든다.
- 회귀 테스트: 빈 DB와 기존 DB 모두에서 `upgrade head`가 성공하고 직후 `alembic check`가 깨끗한지 확인한다.

### 10.3 수정 후 전체 검증 명령

```bash
./scripts/test.sh
```

개별 확인이 필요하면 다음 명령을 사용한다.

```bash
cd backend
../.venv/bin/python -m pytest -q
../.venv/bin/alembic upgrade head
../.venv/bin/alembic check

cd ../frontend
npm test -- --run
npm run build
npm run test:e2e
```

### 10.4 리뷰 시점의 검증 결과

- 백엔드: 78개 테스트 통과
- 프런트엔드: 6개 파일, 58개 테스트 통과
- 프런트엔드 프로덕션 빌드 통과
- Chromium E2E: 3개 테스트 통과
- 빈 DB에서 Alembic `upgrade head` 통과
- Alembic `check`는 위 P3-1의 스키마 drift를 보고함
- 실제 기준 Excel: 8,562개 상품 데이터 행 구조 검증 통과
- 위 P1/P2 결함은 기존 자동 테스트 밖의 별도 API·서비스 재현으로 확인함

### 10.5 남은 외부 검증과 데이터 정책 결정

- 실제 OpenAI API 호출은 비용과 외부 상태 변경을 피하기 위해 리뷰에서 실행하지 않았다. 제공 사진 3장의 실제 추출 결과, 회전·원근 보정과 응답 스키마 호환성을 별도로 검증해야 한다.
- 기준 Excel에는 정수가 아닌 매입단가가 650개 있다. 현재 구현은 이를 값 없음으로 정규화한다. 반올림, 업로드 거부 또는 값 없음 중 어느 정책이 맞는지 제품 결정을 확인해야 한다.
- 기준 Excel의 음수 현재고 678개는 현재 허용되며 기존 테스트에도 허용 정책이 반영되어 있다. 이 정책을 바꾸려면 별도 요구사항 변경으로 처리한다.

### 10.6 다음 Codex의 완료 조건

- P1 세 건을 먼저 수정하고 각 재현을 회귀 테스트로 고정한다.
- P2 네 건과 P3 한 건을 수정하고 기존 테스트를 유지한다.
- `./scripts/test.sh` 전체 통과와 `alembic check` 무변경을 확인한다.
- 실제 OpenAI 추출을 실행하지 않았다면 그 사실을 최종 보고에 명시한다.
- 변경 파일, 테스트 결과, 남은 위험을 `handoff.md` 또는 최종 응답에 갱신한다.

## 11. 2026-08-08 수정 완료와 현재 인계

§10에서 확인한 P1 세 건, P2 네 건, P3 한 건은 모두 수정하고 회귀 테스트를 추가했습니다.

- 작업 단위 `extraction_attempt_id` 소유권과 원자적 상태 획득으로 중복·오래된 추출을 차단했습니다. 추출 중 충돌 mutation과 중단 복구도 같은 규칙으로 정리했습니다.
- 추출·검수 데이터가 생긴 작업의 Excel 교체를 409로 거절하여 기존 매칭·승인·기준값을 보존합니다.
- 사진 단가 편집 시 기본 `매입단가 반영`을 재계산하되, 사용자의 명시적 선택과 단가 무관 편집은 보존합니다.
- 완료 문서와 검수 품목이 없는 작업은 blocker를 반환하고 내보내기를 409로 차단합니다.
- 음수 Excel 매입단가는 업로드 시 422로 거절합니다.
- API datetime은 UTC `Z`로 직렬화하고 화면에서 Asia/Seoul 시각으로 표시합니다.
- 검수 행 자동 저장은 행별 큐와 버전으로 직렬화하여 지연 응답이 최신 입력을 덮어쓰지 않게 했습니다.
- Alembic과 ORM 제약을 동기화하고 `e53a81f0bd21`을 head로 추가했습니다.

전체 `./scripts/test.sh` 결과는 backend 84개, frontend 6개 파일 59개, production build, Chromium E2E 3개가 모두 통과했습니다. 빈 DB와 기존 DB의 `alembic upgrade head` 및 `alembic check`도 통과했습니다. 실제 OpenAI API 호출과 제공 사진 3장의 수동 검수는 비용·외부 상태 변경 때문에 실행하지 않았습니다.

현재 문서 권위와 후속 작업은 다음을 따릅니다.

- 단일 진입점: `docs/00_PROJECT_CONTEXT.md`
- 결정 기록: `docs/03_DECISIONS.md`
- 수용 기준과 증거: `docs/04_ACCEPTANCE_CRITERIA.md`
- 현재 상태: `docs/05_CURRENT_STATUS.md`
- 미완료 작업과 제품 결정: `docs/06_BACKLOG.md`

남은 핵심 제품 결정은 기준 Excel의 정수가 아닌 매입단가 처리 정책입니다. 현재 구현은 이를 값 없음으로 정규화하며, 반올림·업로드 거절·값 없음 유지 중 어느 정책을 채택할지는 Accepted 결정이 아닙니다.
